name: Create JIRA Issue and Branch

on:
  issues:
    types: [opened]

jobs:
  create-issue:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Create JIRA issue
        uses: elisa-actions/jira-issue@v1
        with:
          JIRA_BASE_URL: ${{ secrets.JIRA_BASE_URL }}
          JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}
          JIRA_USER_EMAIL: ${{ secrets.JIRA_USER_EMAIL }}
          JIRA_PROJECT_KEY: ${{ secrets.JIRA_PROJECT_KEY }}
          issue_type: 'Task'
          summary: ${{ github.event.issue.title }}
          description: ${{ github.event.issue.body }}

      - name: Get branch name from issue body
        id: get_branch_name
        run: |
          echo "BRANCH_NAME=$(echo '${{ github.event.issue.body }}' | grep -oP '(?<=🌳 브랜치명 \(Branch\): ).*')" >> $GITHUB_ENV

      - name: Create new branch
        run: |
          git checkout -b "main-${{ env.BRANCH_NAME }}"
          git push origin "main-${{ env.BRANCH_NAME }}"
